import { createAction } from '@ngrx/store';

export const increment = createAction('[Counter Component] Increment Action');
export const decrement = createAction('[Counter Component] Decrement Action');
export const reset = createAction('[Counter Component] Reset Action');
